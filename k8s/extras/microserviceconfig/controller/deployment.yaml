apiVersion: v1
kind: ServiceAccount
metadata:
  name: microserviceconfig-controller
  namespace: platform-audit
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: microserviceconfig-controller
rules:
  - apiGroups: ["labeks.jelsync.hn"]
    resources: ["microserviceconfigs"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["labeks.jelsync.hn"]
    resources: ["microserviceconfigs/status"]
    verbs: ["get", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: microserviceconfig-controller
subjects:
  - kind: ServiceAccount
    name: microserviceconfig-controller
    namespace: platform-audit
roleRef:
  kind: ClusterRole
  name: microserviceconfig-controller
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: microserviceconfig-controller
  namespace: platform-audit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: microserviceconfig-controller
  template:
    metadata:
      labels:
        app: microserviceconfig-controller
    spec:
      serviceAccountName: microserviceconfig-controller
      containers:
        - name: controller
          image: "<CONTROLLER_IMAGE_URI>:latest"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
